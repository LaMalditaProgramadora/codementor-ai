# Este archivo contiene imports para los routers adicionales
# Para implementación completa, cada router debería estar en su propio archivo

from fastapi import APIRouter

# Router para assignments
router_assignments = APIRouter()

# Router para grades  
router_grades = APIRouter()

# Router para plagiarism
router_plagiarism = APIRouter()
